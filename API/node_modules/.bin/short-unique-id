#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../short-unique-id/bin/short-unique-id" "$@"
  ret=$?
else 
  node  "$basedir/../short-unique-id/bin/short-unique-id" "$@"
  ret=$?
fi
exit $ret
